"""Manages different domains used in the application.

Features schemas, interfaces, exceptions and shared utility.

Modules:
    common: Manages entities shared across domains.
    machine: Manages the machine domain.
"""
